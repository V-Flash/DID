<script>
  var input = document.querySelector("#phone");
  var iti = window.intlTelInput(input, {
    initialCountry: "in",
    utilsScript: "js/utils.js",
  });

  // Add event listener to the input element
  input.addEventListener("click", function() {
    // Get the selected country's phone code and input value
    var selectedCountryData = iti.getSelectedCountryData();
    var phoneCode = selectedCountryData.dialCode;
    var inputValue = input.value;

    // Display the input value with the selected country's phone code
    var valueWithCode = "+" + phoneCode + " " + inputValue;
    alert("Input value with country code: " + valueWithCode);
  });
</script>
