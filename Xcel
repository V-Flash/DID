foreach ($categories as $category) {
    if ($category->name == 'Uncategorized') {
        continue;
    }
    
    // Add debugging output
    echo 'Category Name: ' . esc_html($category->name) . '<br>';
    echo 'Category Count: ' . esc_html($category->count) . '<br>';
    
    // Rest of your code
    // ...
}
