<?php
function getProductPrice($product) {
    if ($product->get_sale_price()) {
        return $product->get_sale_price();
    } else {
        return $product->get_regular_price();
    }
}

// Example usage:
$product = /* your product object here */;
$price = getProductPrice($product);
echo $price;
?>
