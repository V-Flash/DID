<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <style>
    body {
      margin: 0;
      overflow: hidden;
      font-family: 'Arial', sans-serif;
      display: flex;
      align-items: center;
      justify-content: center;
      height: 100vh;
    }

    .curved-text {
      font-size: 2em;
      white-space: nowrap;
      opacity: 1;
      transition: opacity 0.5s ease;
    }

    .bend {
      transform: perspective(500px) rotateX(-20deg);
    }
  </style>
</head>
<body>
  <div class="curved-text">Give your people a platform</div>

  <script>
    document.addEventListener("scroll", function() {
      let scrollPosition = window.scrollY;
      let curvedText = document.querySelector(".curved-text");

      let opacity = 1 - scrollPosition / 500;

      curvedText.style.opacity = opacity;

      if (scrollPosition > 100) {
        curvedText.classList.add("bend");
      } else {
        curvedText.classList.remove("bend");
      }
    });
  </script>
</body>
</html>
