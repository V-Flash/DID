jQuery(document).ready(function($) {
    $('.wqpmb_input_text').on('change keyup paste', function () {
        var inputValue = $(this).val();
        $(this).closest('.product').find('.ut_single_cart_btn').attr("data-quantity", inputValue);
    });
});
