add_filter('woocommerce_product_get_weight', 'convert_weight_to_kg', 10, 2);

function convert_weight_to_kg($weight, $product) {
    if ($weight >= 1000) {
        $weight = $weight / 1000; // Convert grams to kilograms
    }
    return $weight;
}
