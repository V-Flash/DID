/* Override Bootstrap's default modal fade animation */
.modal.fade .modal-dialog {
  transform: translateY(-100%);
  transition: transform 0.5s ease-in-out;
}

.modal.fade.show .modal-dialog {
  transform: translateY(0);
}
