<?php
                
                $faqcatdata = getallfaqcat();
                $f = 1;
                for($i=0;$i<count($faqcatdata);$i++){
                $fcat = getallfaqanswerbycategory($faqcatdata[$i]["id"]);

                for($a = 0 ;$a<count($fcat);$a++){
                $titleanswer=$fcat[$a]["title"];
                    }}
                ?>
