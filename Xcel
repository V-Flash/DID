jQuery(document).ready(function($) {
  for (var i = 1; i <= anchorElements.length; i++) {
    if (i % 2 == 1) {
      $('#intest' + i).on('change keyup paste', function () {
        $('#test' + ((i + 1) / 2)).attr("data-quantity", $('#intest' + i).val());
      });
    }
  }
});
