add_filter('woocommerce_format_weight', 'custom_weight_format', 10, 2);

function custom_weight_format($formatted_weight, $weight) {
    if ($weight >= 1000) {
        $formatted_weight = ($weight / 1000) . ' kg';
    }
    return $formatted_weight;
}
