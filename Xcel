<?php
if (has_post_thumbnail($loop->post->ID)) {
    $thumbnail_url = get_the_post_thumbnail_url($loop->post->ID, 'shop_catalog');
    echo '<img src="' . esc_url($thumbnail_url) . '" alt="' . esc_attr(get_the_title()) . '" />';
}
?>
