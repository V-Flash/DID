$('.zoom-img').on('click', function() {
  var images = $('.zoom');
  var currentIndex = images.index(this);

  $.fancybox.open(images, {
    thumbs: {
      autoStart: true,
      axis: 'y'
    },
    loop: true,
    index: currentIndex
  });

  return false;
});
