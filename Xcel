add_filter('woocommerce_get_weight', 'remove_default_weight_unit', 10, 2);

function remove_default_weight_unit($weight, $product) {
    // Set weight to the unformatted value (no unit)
    return $weight;
}
