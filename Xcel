add_filter('woocommerce_get_weight', 'custom_get_weight', 10, 2);

function custom_get_weight($weight, $product) {
    $unit = 'gm';
    if ($weight >= 1000) {
        $weight = $weight / 1000;
        $unit = 'kg';
    }
    return $weight . ' ' . $unit;
}
